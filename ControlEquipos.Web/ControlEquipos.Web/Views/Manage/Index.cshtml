@model ControlEquipos.Web.Models.IndexViewModel
@{
    ViewBag.Title = "Manage";
}

<center>
    <div class="card w-75 bg-dark m-5">
        <div class="card-body bg-gradient-dark">
            <h1 class="card-title text-center text-danger"><b>AVISO CAMBIO DE CONTRASEÑA</b></h1><hr />
            <h4 class="card-text text-justify text-white">
                Estas en la opcion para cambiar tu contraña actual,
                ¿Estas realmente seguro de lo que vas a hacer?
            </h4>
            <br />
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
        <div class="form-actions container">
            @Html.ActionLink("Cambiar mi Contrseña", "ChangePassword", null, new { @class = "btn btn-outline-danger btn-lg text-center float-center" })
        </div>
            }
        </div>
    </div>

</center>